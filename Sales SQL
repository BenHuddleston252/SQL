#In this SQL, I'm querying a database with multiple tables to quantify customer and order data statistics.


-- How many orders did we get in January?
SELECT COUNT(distinct orderID)
FROM JanSales
WHERE length(orderID)=6
AND orderID<>'Order ID';


-- How many of those January orders were for an Iphone?
SELECT COUNT(distinct orderID) iphone
FROM JanSales
WHERE Product='iPhone'
AND length(orderID)=6
AND Product<>'Product';

-- Select the customer account numbers for all the orders that were placed in February.
SELECT distinct acctnum
FROM customers 
JOIN FebSales 
ON customers.order_ID=febsales.orderID
WHERE febsales.orderID<>'Order ID'
AND length(febsales.orderID)=6;

-- Which product was the cheapest one sold in January, and what was the price?
SELECT DISTINCT product
,MIN(price) cheapest
FROM JanSales;

-- What is the total revenue for each product sold in January? (Revenue can be calculated using the number of products sold and the price of the products).
SELECT product,
ROUND(SUM(quantity*price)) revenue
FROM JanSales
WHERE product<>'Product'
GROUP BY product;

-- Which products were sold in February at 548 Lincoln St, Seattle, WA 98101, how many of each were sold, and what was the total revenue?
SELECT product
,quantity
,SUM(quantity*price) revenue
FROM FebSales
WHERE location='548 Lincoln St, Seattle, WA 98101';

-- How many customers ordered more than 2 products at a time in February, and what was the average amount spent for those customers?
SELECT COUNT(distinct acctnum) customer 
,ROUND((SUM(quantity*price)/COUNT(acctnum))) spent
FROM FebSales
LEFT JOIN customers
ON febsales.orderID=customers.order_id

-- List all the products sold in Los Angeles in February, and include how many sold each.
SELECT product
,SUM(Quantity)
,location
FROM Febsales
WHERE location like '%Los Angeles%'
GROUP BY product;

-- Which locations in New York received at least 3 orders in January, and how many orders did they each receive?
SELECT distinct location, COUNT(orderID) 
FROM jansales
WHERE location like '%NY%'
AND length(orderID)=6
AND orderID<>'Order ID'
GROUP BY location
HAVING COUNT(orderID)>=3;

-- How many of each type of headphones were sold in February?
SELECT distinct product, SUM(quantity) AS quantity
FROM febsales
WHERE product like '%headphone%'
GROUP BY product;

-- What was the average amount spent per account in February?
SELECT ROUND(SUM(quantity*price)/COUNT(acctnum)) AS average_spent
FROM febsales
JOIN customers
ON febsales.orderID=customers.order_id
WHERE order_id<>'Order ID'
AND length(order_id)=6;

-- What was the average quantity of products purchased per account in February? (Hint: just like question 3, we want the overall average, not the average for each account individually).
SELECT SUM(quantity)/COUNT(acctnum) AS avg_products
FROM febsales
JOIN customers
ON febsales.orderID=customers.order_id
WHERE orderid<>'Order ID'
AND length(orderID)=6;

-- Which product brought in the most revenue in January and how much revenue did it bring in total?
SELECT product, SUM(quantity*price) AS revenue
FROM jansales
GROUP BY product
ORDER BY revenue DESC
LIMIT 1;

